import { BACKGROUND_ON, BACKGROUND_OFF } from "../constants/constants";

export const switchBackgroundOn = () => ({ type: BACKGROUND_ON });

export const switchBackgroundOff = () => ({ type: BACKGROUND_OFF });
